workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      groups:
        - appstore
      vars:
        EXPO_PUBLIC_DOMAIN: "doctoritup"
      node: v18
      xcode: 16.2
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Set up Expo
        script: |
          npx expo install expo-dev-client
      - name: Prebuild iOS
        script: |
          npx expo prebuild --platform ios --clean
          cd ios && pod install && cd ..
      - name: Set up code signing
        script: |
          app-store-connect fetch-signing-files "Com.owlkimmy.doctoritup" \
            --type IOS_APP_STORE \
            --create
          keychain initialize
          keychain add-certificates
      - name: Use profiles
        script: |
          xcode-project use-profiles
      - name: Build iOS
        script: |
          NODE_PATH=$(which node)
          echo "Node found at: $NODE_PATH"
          echo "export NODE_BINARY=$NODE_PATH" > ios/.xcode.env.local
          echo "export FORCE_BUNDLING=1" >> ios/.xcode.env.local
          cat ios/.xcode.env.local
          
          # Copy node binary to /usr/local/bin so Xcode scripts can find it
          cp "$NODE_PATH" /usr/local/bin/node 2>/dev/null || true
          
          WORKSPACE=$(find ios -name "*.xcworkspace" -maxdepth 1 | head -1)
          SCHEME=$(basename "$WORKSPACE" .xcworkspace)
          echo "Using workspace: $WORKSPACE, scheme: $SCHEME"
          xcode-project build-ipa \
            --workspace "$WORKSPACE" \
            --scheme "$SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - App Store Connect Users
